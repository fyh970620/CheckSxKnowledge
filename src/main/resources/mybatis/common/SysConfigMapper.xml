<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffcs.itm.web.common.repository.SysConfigMapper">
	<!-- 获取系统配置值 -->
	<select id="getSysValue" resultType="String">
		SELECT SYS_VAR_VALUE FROM SYS_CONFIG WHERE SYS_VAR = #{sysVar}
	</select>
	
	<!-- 更新系统配置值 -->
	<update id="updateSysValue">
		UPDATE SYS_CONFIG
		   SET SYS_VAR_VALUE = #{value}
		 WHERE SYS_VAR = #{sysVar}
	</update>
</mapper>